import 'package:flutter/material.dart';

class StudentMemberLoginPage extends StatelessWidget {
  const StudentMemberLoginPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // Material App
    return const MaterialApp(

      // Scaffold Widget
        home: Scaffold(
          body: Center(child: Text("STUDENT MEMBER")),
        ));
  }
}
